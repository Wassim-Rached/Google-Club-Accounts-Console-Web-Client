import { RolePublicNamePipe } from './role-public-name.pipe';

describe('RolePublicNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RolePublicNamePipe();
    expect(pipe).toBeTruthy();
  });
});
