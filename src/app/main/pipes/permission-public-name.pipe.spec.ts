import { PermissionPublicNamePipe } from './permission-public-name.pipe';

describe('PermissionPublicNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionPublicNamePipe();
    expect(pipe).toBeTruthy();
  });
});
